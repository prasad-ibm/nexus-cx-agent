{
  "build": { "builder": "NIXPACKS" },
  "deploy": {
    "startCommand": "node_modules/.bin/next start -p ${PORT:-3000} -H 0.0.0.0",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE"
  }
}
